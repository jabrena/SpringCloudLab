spring:
  application:
    name: Cobaya-v1

eureka:
  instance:
    metadataMap:
      instanceId: ${spring.application.name}
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/